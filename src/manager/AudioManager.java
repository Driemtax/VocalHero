package manager;

public class AudioManager {
    // getAudioBuffer() method to get the audio data
    //getSampleRate() method to get the sample rate

    // We also need some methods here for playing, recording audio. This will be triggered by the UI
    // via the TrainingController.
    // 1. startRecording(Mixer.Info mixerInfo)
    // 2. playRecording(Mixer.Info mixerInfo)
    // 3. playNote(Note note)

    // Some Helper methods to convert between frequencies and notes
    // 1. frequencyToMidiNote(double frequency)


}
